---
phase: 03-frontend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/CategorySidebar.tsx
  - frontend/src/components/CategoryTree.tsx
  - frontend/src/components/VideoGrid.tsx
  - frontend/src/store/index.ts
autonomous: true
requirements:
  - UI-CAT-01
  - UI-CAT-02
  - UI-CAT-03
  - UI-CAT-04
  - UI-CAT-05
user_setup: []

must_haves:
  truths:
    - User can view categories in sidebar tree structure
    - User can expand/collapse category children
    - User can add new categories
  artifacts:
    - path: frontend/src/components/CategorySidebar.tsx
      provides: Sidebar with category tree
      min_lines: 50
    - path: frontend/src/components/CategoryTree.tsx
      provides: Recursive tree rendering with expand/collapse
      min_lines: 40
  key_links:
    - from: frontend/src/store/index.ts
      to: frontend/src/components/CategorySidebar.tsx
      via: Zustand store import
      pattern: import.*useStore
---

<objective>
Build Category Sidebar with Tree UI and Category CRUD forms
</objective>

<context>
@.planning/phases/03-frontend-core/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@frontend/src/App.tsx
@frontend/src/store/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CategorySidebar component with tree structure</name>
  <read_first>
    - frontend/src/App.tsx (main app layout)
    - frontend/src/store/index.ts (Zustand store for categories)
  </read_first>
  <files>
    - frontend/src/components/CategorySidebar.tsx
  </files>
  <action>
Create CategorySidebar.tsx with:
- Left sidebar layout (~250px width, fixed position)
- Imports categories from Zustand store
- Renders category list with expand/collapse chevrons
- Indent children by 20px per level
- Click handler to toggle expand state
- Uses Airtable design colors from PAGE-DESIGN.md:
  - Deep Navy (#181d26) for primary text
  - 12px radius for buttons, 16-24px for cards
- Uses Lucide React icons (ChevronRight, ChevronDown)
- Smooth expand/collapse animation (150ms)
  </action>
  <verify>
    <automated>test -f frontend/src/components/CategorySidebar.tsx && echo "File exists" || echo "Missing"</automated>
  </verify>
  <done>
CategorySidebar renders categories in tree with expand/collapse
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryTree recursive component</name>
  <read_first>
    - frontend/src/components/CategorySidebar.tsx
  </read_first>
  <files>
    - frontend/src/components/CategoryTree.tsx
  </files>
  <action>
Create CategoryTree.tsx with:
- Recursive rendering for nested categories
- Props: category: Category, depth: number
- Chevron icons (▶/▼) indicating expand state
- Indent children by ~20px per level
- Click on category name toggles children visibility
- Click on chevron toggles expand/collapse
  </action>
  <verify>
    <automated>test -f frontend/src/components/CategoryTree.tsx && echo "File exists" || echo "Missing"</automated>
  </verify>
  <done>
CategoryTree recursively renders nested categories
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CategoryModal for add/edit operations</name>
  <read_first>
    - frontend/src/components/CategorySidebar.tsx
  </read_first>
  <files>
    - frontend/src/components/CategoryModal.tsx
  </files>
  <action>
Create CategoryModal.tsx with:
- Centered modal with backdrop overlay (rgba(0,0,0,0.5))
- Form fields: name (required), parent (optional dropdown)
- Form validation with inline error messages
- Submit calls API: POST /api/categories
- Cancel button closes modal
- Uses Airtable Blue (#1b61c9) for CTA button
- 16px radius for modal card
  </action>
  <verify>
    <automated>test -f frontend/src/components/CategoryModal.tsx && echo "File exists" || echo "Missing"</automated>
  </verify>
  <done>
CategoryModal opens centered with form, validates input
  </done>
</task>

<task type="auto">
  <name>Task 4: Create ConfirmDialog for delete operations</name>
  <read_first>
    - frontend/src/components/CategoryModal.tsx
  </read_first>
  <files>
    - frontend/src/components/ConfirmDialog.tsx
  </files>
  <action>
Create ConfirmDialog.tsx with:
- Centered modal with backdrop overlay
- Warning message: "Are you sure you want to delete [name]?"
- Subtext: "This will also delete all child categories and videos."
- Two buttons: Cancel (gray), Delete (red/danger)
- Confirm calls API: DELETE /api/categories/:id
- Uses Lucide React icons (AlertTriangle)
  </action>
  <verify>
    <automated>test -f frontend/src/components/ConfirmDialog.tsx && echo "File exists" || echo "Missing"</automated>
  </verify>
  <done>
ConfirmDialog shows delete warning with confirmation
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire Category components in App.tsx</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/components/CategorySidebar.tsx
    - frontend/src/components/CategoryModal.tsx
    - frontend/src/components/ConfirmDialog.tsx
  </read_first>
  <files>
    - frontend/src/App.tsx
  </files>
  <action>
Update App.tsx to:
- Import CategorySidebar, CategoryModal, ConfirmDialog
- Add state for modal visibility (showCategoryModal, showDeleteConfirm)
- Add "Add Category" button in sidebar header
- Add category selection state for edit/delete actions
- Wire Add button to open CategoryModal
- Wire edit/delete icons in sidebar to open respective modals
- Render modal components conditionally based on state
- Ensure sidebar (~250px) and content area layout
  </action>
  <verify>
    <automated>grep -q "CategorySidebar" frontend/src/App.tsx && echo "Imported" || echo "Missing import"</automated>
  </verify>
  <done>
App.tsx renders CategorySidebar with modal triggers
  </done>
</task>

</tasks>

<verification>
- CategorySidebar renders in left sidebar
- Category tree shows expand/collapse on click
- Add Category button opens modal
- Edit/Delete actions work via modals
</verification>

<success_criteria>
- User can view categories in sidebar tree structure (UI-CAT-01)
- User can expand/collapse category children (UI-CAT-02)
- User can add new categories (UI-CAT-03)
- User can edit category (UI-CAT-04)
- User can delete category with confirmation (UI-CAT-05)
</success_criteria>

<output>
After completion, create .planning/phases/03-frontend-core/03-01-SUMMARY.md
</output>
