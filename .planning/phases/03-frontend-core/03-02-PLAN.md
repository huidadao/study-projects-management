---
phase: 03-frontend-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/VideoGrid.tsx
  - frontend/src/components/VideoModal.tsx
  - frontend/src/store/index.ts
autonomous: true
requirements:
  - UI-VID-01
  - UI-VID-02
  - UI-VID-03
  - UI-VID-04
user_setup: []

must_haves:
  truths:
    - User can view videos in card grid
    - User can add new videos with category selection
    - User can toggle watched/unwatched status
  artifacts:
    - path: frontend/src/components/VideoGrid.tsx
      provides: Card grid display for videos
      min_lines: 50
    - path: frontend/src/components/VideoModal.tsx
      provides: Video form modal for add/edit
      min_lines: 40
  key_links:
    - from: frontend/src/store/index.ts
      to: frontend/src/components/VideoGrid.tsx
      via: Zustand store import
      pattern: import.*useStore
---

<objective>
Build Video Card Grid and Video Management Forms
</objective>

<context>
@.planning/phases/03-frontend-core/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@frontend/src/App.tsx
@frontend/src/store/index.ts
@frontend/src/components/CategorySidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VideoGrid component with card display</name>
  <read_first>
    - frontend/src/App.tsx (main app layout)
    - frontend/src/store/index.ts (Zustand store for videos)
  </read_first>
  <files>
    - frontend/src/components/VideoGrid.tsx
  </files>
  <action>
Create VideoGrid.tsx with:
- CSS Grid layout (responsive: 1 col mobile, 2 col tablet, 3-4 col desktop)
- Card component for each video showing:
  - Video title (truncated if too long)
  - Video URL (clickable link to YouTube)
  - Category badge/tag
  - Watched status indicator (checkmark icon if watched)
  - Edit and Delete action buttons
- Uses Airtable design colors from PAGE-DESIGN.md:
  - Deep Navy (#181d26) for primary text
  - 12px radius for buttons, 16-24px for cards
- Uses Lucide React icons (Play, Check, Edit, Trash, ExternalLink)
- Empty state when no videos exist
  </action>
  <verify>
    <automated>test -f frontend/src/components/VideoGrid.tsx && echo "File exists" || echo "Missing"</automated>
  </verify>
  <done>
VideoGrid displays videos in responsive card grid
  </done>
</task>

<task type="auto">
  <name>Task 2: Create VideoModal for add/edit video operations</name>
  <read_first>
    - frontend/src/components/VideoGrid.tsx
    - frontend/src/components/CategorySidebar.tsx
  </read_first>
  <files>
    - frontend/src/components/VideoModal.tsx
  </files>
  <action>
Create VideoModal.tsx with:
- Centered modal with backdrop overlay (rgba(0,0,0,0.5))
- Form fields:
  - Title (required, text input)
  - URL (required, YouTube URL validation)
  - Category (required, dropdown from categories)
- Form validation with inline error messages
- Submit calls API: POST /api/videos (create) or PUT /api/videos/:id (edit)
- Cancel button closes modal
- Uses Airtable Blue (#1b61c9) for CTA button
- 16px radius for modal card
- For edit mode: pre-fill form with existing video data
  </action>
  <verify>
    <automated>test -f frontend/src/components/VideoModal.tsx && echo "File exists" || echo "Missing"</automated>
  </verify>
  <done>
VideoModal opens with form, validates input, category dropdown works
  </done>
</task>

<task type="auto">
  <name>Task 3: Add watched toggle functionality</name>
  <read_first>
    - frontend/src/components/VideoGrid.tsx
    - frontend/src/store/index.ts
  </read_first>
  <files>
    - frontend/src/components/VideoGrid.tsx
    - frontend/src/store/index.ts
  </files>
  <action>
Add watched toggle to VideoGrid:
- Add clickable watched status indicator on each card
- Toggle calls API: PUT /api/videos/:id with {watched: boolean}
- Optimistic UI update via Zustand store
- Check icon (Lucide Check) shows when watched, outline when not
- Uses Airtable Blue (#1b61c9) for checked state
  </action>
  <verify>
    <automated>grep -q "watched" frontend/src/components/VideoGrid.tsx && echo "Watched toggle exists" || echo "Missing"</automated>
  </verify>
  <done>
User can click to toggle watched/unwatched status
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire Video components in App.tsx</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/components/VideoGrid.tsx
    - frontend/src/components/VideoModal.tsx
    - frontend/src/components/ConfirmDialog.tsx
  </read_first>
  <files>
    - frontend/src/App.tsx
  </files>
  <action>
Update App.tsx to:
- Import VideoGrid, VideoModal, ConfirmDialog
- Add state for video modal visibility (showVideoModal, showDeleteConfirm)
- Add "Add Video" button in main content area header
- Add video selection state for edit/delete actions
- Wire Add button to open VideoModal (empty form)
- Wire edit button on video card to open VideoModal (pre-filled)
- Wire delete button on video card to open ConfirmDialog
- Wire watched toggle to update status
- Render modal components conditionally based on state
  </action>
  <verify>
    <automated>grep -q "VideoGrid" frontend/src/App.tsx && echo "Imported" || echo "Missing import"</automated>
  </verify>
  <done>
App.tsx renders VideoGrid with modal triggers and watched toggle
  </done>
</task>

</tasks>

<verification>
- VideoGrid renders in main content area
- Video cards show title, URL, category, watched status
- Add Video button opens modal
- Edit/Delete actions work via modals
- Watched toggle updates status on click
</verification>

<success_criteria>
- User can view videos in card grid format (UI-VID-01)
- User can add new video with category selection (UI-VID-02)
- User can toggle watched/unwatched status (UI-VID-03)
- User can edit/delete videos (UI-VID-04)
</success_criteria>

<output>
After completion, create .planning/phases/03-frontend-core/03-02-SUMMARY.md
</output>
