---
phase: 01-project-setup
plan: "02"
type: execute
wave: "1"
depends_on: []
files_modified: []
autonomous: true
requirements:
  - SETUP-02
  - SETUP-03
must_haves:
  truths:
    - React frontend builds successfully
    - Vite dev server runs on port 5173
    - Tailwind CSS configured
    - Zustand for state management
    - Backend and frontend communicate via CORS
  artifacts:
    - path: frontend/src/App.tsx
      provides: React root component
    - path: frontend/tailwind.config.js
      provides: Tailwind CSS configuration
    - path: frontend/src/store/index.ts
      provides: Zustand store for state management
  key_links:
    - from: frontend/src/App.tsx
      to: frontend/src/store/index.ts
      via: useStore hook
      pattern: useStore
---

<objective>
Initialize React + Vite frontend with TypeScript and setup Tailwind CSS and Zustand.

Purpose: Create the frontend foundation that will display categories and videos UI.
Output: Runnable React application on port 5173.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize React + Vite project with TypeScript</name>
  <files>frontend/src/App.tsx, frontend/src/main.tsx, frontend/index.html, frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts</files>
  <action>
1. Create `frontend/` directory structure:
   - `frontend/src/`
   - `frontend/public/`
2. Create `frontend/package.json`:
   - name: "youtube-learning-tracker"
   - private: true
   - type: module
   - Add dependencies: react, react-dom, zustand
   - Add devDependencies: vite, @vitejs/plugin-react, typescript, @types/react, @types/react-dom
3. Create `frontend/vite.config.ts`:
   - Import defineConfig from vite
   - Use @vitejs/plugin-react
   - Configure server port 5173
4. Create `frontend/tsconfig.json`:
   - Standard React+TS config for Vite
5. Create `frontend/index.html`:
   - Standard Vite HTML template with <div id="root">
6. Create `frontend/src/main.tsx`:
   - Import ReactDOM from react-dom/client
   - Render App in root div
7. Create `frontend/src/App.tsx`:
   - Basic React component returning <h1>YouTube Learning Tracker</h1>
8. Create `frontend/src/vite-env.d.ts` for TypeScript reference types
9. Create `frontend/src/__init__.py` (can be empty for Python tooling compatibility if needed)
</action>
  <verify>
    <automated>cd frontend && npm install && npm run build</automated>
  </verify>
  <done>Frontend project created, npm install succeeds, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS</name>
  <files>frontend/tailwind.config.js, frontend/postcss.config.js, frontend/src/index.css</files>
  <action>
1. Install Tailwind and dependencies:
   - npm install -D tailwindcss postcss autoprefixer
   - npx tailwindcss init -p
2. Configure `frontend/tailwind.config.js`:
   - content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
   - theme: extend with project colors if needed
3. Create `frontend/postcss.config.js`:
   - plugins: { tailwindcss: {}, autoprefixer: {} }
4. Create `frontend/src/index.css`:
   - Add @tailwind directives for base, components, utilities
5. Import index.css in main.tsx
</action>
  <verify>
    <automated>cd frontend && npx tailwindcss init -p && test -f tailwind.config.js</automated>
  </verify>
  <done>Tailwind CSS configured, styles apply to App component.</done>
</task>

<task type="auto">
  <name>Task 3: Set up Zustand store</name>
  <files>frontend/src/store/index.ts</files>
  <action>
1. Create `frontend/src/store/` directory
2. Create `frontend/src/store/index.ts`:
   - Import create from zustand
   - Define store interface (empty for now, will add types in later phases)
   - Create store with create<AppState>()
   - Export useStore hook
3. Update App.tsx to import and use the store to verify it works
</action>
  <verify>
    <automated>cd frontend && npm run build | grep -q "success"</automated>
  </verify>
  <done>Zustand store created and imported in App.tsx.</done>
</task>

<task type="auto">
  <name>Task 4: Verify frontend runs and communicates with backend</name>
  <files>frontend/src/App.tsx</files>
  <action>
1. Update App.tsx to fetch from backend on mount:
   - useEffect to fetch http://localhost:8000/
   - Display the backend response
2. Start frontend: `npm run dev -- --host 0.0.0.0`
3. Test: open browser to http://localhost:5173
4. Verify CORS is working (no console errors about CORS)
</action>
  <verify>
    <automated>curl -s http://localhost:5173 | grep -q "YouTube Learning Tracker"</automated>
  </verify>
  <done>Frontend runs on port 5173, fetches from backend via CORS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input from frontend crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-01-03 | I | CORS | accept | Backend restricts to localhost:5173 only |
</threat_model>

<verification>
1. Frontend builds without errors
2. Vite dev server runs on port 5173
3. App component displays in browser
4. Backend responds to frontend requests via CORS
</verification>

<success_criteria>
- React frontend builds and runs on port 5173
- Tailwind CSS configured and working
- Zustand for state management
- Frontend can communicate with backend via CORS
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md`
</output>