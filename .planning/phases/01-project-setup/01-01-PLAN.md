---
phase: 01-project-setup
plan: "01"
type: execute
wave: "1"
depends_on: []
files_modified: []
autonomous: true
requirements:
  - SETUP-01
must_haves:
  truths:
    - FastAPI backend runs on port 8000
    - SQLModel is configured with SQLite
    - CORS enabled for frontend origin
  artifacts:
    - path: backend/main.py
      provides: FastAPI application entry point
    - path: backend/database.py
      provides: SQLModel session and engine
    - path: backend/models.py
      provides: SQLModel base and table configs
  key_links:
    - from: backend/main.py
      to: backend/database.py
      via: import Database
      pattern: from.*database import
---

<objective>
Initialize FastAPI backend project with SQLModel and Pydantic v2.

Purpose: Create the backend foundation that will serve the API endpoints for categories and videos.
Output: Runnable FastAPI application on port 8000.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend directory structure and initialize project</name>
  <files>backend/main.py, backend/__init__.py, backend/database.py, backend/models.py</files>
  <action>
1. Create `backend/` directory with `__init__.py`
2. Create `backend/database.py` with SQLModel session setup:
   - Import SQLModel, create_engine, create_all
   - Use SQLite at `backend/data.db`
   - Export `engine`, `Session`, `get_session`
3. Create `backend/models.py` with SQLModel base:
   - Import SQLModel
   - Define empty base for table models
4. Create `backend/main.py` with FastAPI app:
   - Import FastAPI, SQLModel, CORSMiddleware
   - Create app = FastAPI()
   - Add CORS middleware allowing http://localhost:5173
   - Add root GET / returning {"message": "Backend running"}
   - Add health GET /health returning {"status": "ok"}
</action>
  <verify>
    <automated>python -c "from backend.main import app; print('FastAPI app imports successfully')"</automated>
  </verify>
  <done>Backend directory created with main.py, database.py, models.py. FastAPI app can be imported.</done>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt and verify backend runs</name>
  <files>backend/requirements.txt</files>
  <action>
1. Create `backend/requirements.txt` with:
   - fastapi
   - uvicorn
   - sqlmodel
   - pydantic>=2.0
2. Install dependencies: `pip install -r backend/requirements.txt`
3. Start server in background: `cd backend && uvicorn main:app --host 0.0.0.0 --port 8000 &`
4. Wait 3 seconds, then test: `curl -s http://localhost:8000/`
5. Test health: `curl -s http://localhost:8000/health`
</action>
  <verify>
    <automated>curl -s http://localhost:8000/ | grep -q "Backend running" && curl -s http://localhost:8000/health | grep -q "ok"</automated>
  </verify>
  <done>Backend runs on port 8000, responds to / and /health endpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input from frontend crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | CORS | mitigate | Restrict CORS to specific frontend origin (localhost:5173) only |
| T-01-02 | I | API Input | accept | Pydantic v2 validation on all endpoints |
</threat_model>

<verification>
1. Backend imports without errors
2. Server starts on port 8000
3. GET / returns {"message": "Backend running"}
4. GET /health returns {"status": "ok"}
</verification>

<success_criteria>
- FastAPI backend runs locally on port 8000
- SQLModel configured with SQLite database
- CORS enabled for frontend origin (localhost:5173)
- Health endpoint returns 200
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>